<Page
    x:Class="CostMasterAI.Views.IngredientsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CostMasterAI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Supply Room 📦" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,24"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12" Margin="0,0,0,24">
            <TextBox Header="Nama Bahan" Text="{Binding NewName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="200"/>
            <TextBox Header="Harga (Rp)" Text="{Binding NewPrice, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
            <TextBox Header="Berat/Isi" Text="{Binding NewQty, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="100"/>
            <ComboBox Header="Satuan" SelectedItem="{Binding NewUnit, Mode=TwoWay}" Width="100">
                <x:String>Gram</x:String>
                <x:String>ML</x:String>
                <x:String>Pcs</x:String>
                <x:String>Kg</x:String>
            </ComboBox>
            <Button Content="Simpan" Command="{Binding AddIngredientCommand}" VerticalAlignment="Bottom" Style="{StaticResource AccentButtonStyle}"/>
        </StackPanel>

        <controls:DataGrid Grid.Row="2"
                           ItemsSource="{Binding Ingredients}"
                           AutoGenerateColumns="False"
                           GridLinesVisibility="Horizontal">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <controls:DataGridTextColumn Header="Nama Bahan" Binding="{Binding Name}" Width="*"/>
                <controls:DataGridTextColumn Header="Harga Beli" Binding="{Binding PricePerPackage}"/>
                <controls:DataGridTextColumn Header="Qty" Binding="{Binding QuantityPerPackage}"/>
                <controls:DataGridTextColumn Header="Unit" Binding="{Binding Unit}"/>
                <controls:DataGridTextColumn Header="Harga/Unit" Binding="{Binding PricePerUnit}" Foreground="Green"/>

                <controls:DataGridTemplateColumn Width="80">
                    <controls:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="❌" 
                                    Command="{Binding DataContext.DeleteIngredientCommand, ElementName=ParentPage}"
                                    CommandParameter="{Binding}"
                                    Background="Transparent" BorderThickness="0"/>
                        </DataTemplate>
                    </controls:DataGridTemplateColumn.CellTemplate>
                </controls:DataGridTemplateColumn>
            </controls:DataGrid.Columns>
        </controls:DataGrid>
    </Grid>
</Page>